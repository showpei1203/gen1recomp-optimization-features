# SOULGOLD SHOWDOWN MAINLINE HANDOVER — M2R12B
Date: 2026-08-31

## Current accepted baseline
- M1.4: SEALED
- M2R5D: presentation accepted
- M2R8F: runtime/presentation accepted
- M2R11E: visual presentation accepted by user, including healthbox alpha-mask fix

## Current candidate
**M2R12B Full Gen1-2 Scaleout**

Run:
`tools\soulgold_mgba\START_M2R12B_FULL_GEN1_2_SCALEOUT.bat`

## M2R12B blocker fixed
M2R12A reached 250/251 heights and failed only on `201:UNOWN`. SoulGold defines Unown through `UNOWN_MISC_INFO(...)` rather than an ordinary SpeciesInfo block. M2R12B resolves constant height/weight fields from SpeciesInfo helper macros.

## Permanent scale rules
XS 0.60 / S 0.72 / M 0.84 / L 0.96 / XL-Huge 1.08 / XXL-Colossal 1.20. Size class is the sole base-scale authority. Geometry exceptions may change fit/anchor only.

## Permanent presentation rules
- native battle engine remains correctness/timeline authority;
- Showdown is presentation replacement only;
- player HUD is above opponent FRONT only;
- player BACK is not forced behind opponent HUD;
- player healthbox restoration is alpha-masked, never rectangular framebuffer paste;
- HUD bounce does not move player sprite;
- native battler fallback remains safe when provider is unavailable.

## Next after M2R12B runtime acceptance
Move to M3/M5 presentation work: switch lifecycle + action presentation.

### Switch presentation
Visual lifecycle for changing battlers without changing battle mechanics: outgoing Showdown sprite withdraws/revokes cleanly, incoming species/provider is acquired atomically, no old native/Showdown sprite flashes, HUD/species ownership stays synchronized, and send-out/return/faint transitions remain driven by native battle state.

### Action presentation
Use existing native battle/move state to select Showdown body/action animation where assets support it: attack/lunge/body motion, hit reaction, faint/return states, etc. It must NOT create a second gameplay clock; move timing, damage, sound and battle correctness remain native/mGBA authority.

## Do not reopen
- PMD branch before Showdown-first mainline is complete.
- clean-plate framebuffer suppression.
- global HUD-over-all-battlers rule.
- per-species manual scale as base authority.