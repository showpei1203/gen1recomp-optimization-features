name: PMD SoulGold G1 Legacy Compile

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded workflow
        run: |
          echo "This legacy workflow is intentionally manual-only."
          echo "Authoritative G1 compile gate: .github/workflows/soulgold-pmd-g1-build.yml"
