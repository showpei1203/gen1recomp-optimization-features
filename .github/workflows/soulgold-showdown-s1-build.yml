name: SoulGold Showdown S1 Transition Gate

on:
  workflow_dispatch:

jobs:
  transition-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Push builds temporarily paused while S1B B-button harness is replaced by the natural starter/first-battle path."
