name: SoulGold Showdown S1 Build Gate

on:
  workflow_dispatch:

# Temporarily dispatch-only while S1B deterministic B-button harness is staged.
# Push triggering is restored in the final S1B workflow commit.

env:
  SOULGOLD_REV: b5122bdf188943862c13abe4938e88b7bb3c5c4a
  ROM_PATH: soulgold/Soulgold_Beta_1.gba
  SHOWDOWN_SPRITES_URL: https://www.pokemonshowdown.com/files/resources/sprites.zip

jobs:
  compile-s1:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Staging guard
        run: echo "S1B staging in progress; final push workflow will restore the full build gate."
