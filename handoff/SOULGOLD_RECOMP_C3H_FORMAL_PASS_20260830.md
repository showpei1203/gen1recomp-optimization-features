# SoulGoldRecomp C3H Formal Pass / Sealed Interactive Baseline

Date: 2026-08-30

## Promotion

`S0-C3H = FORMAL PASS / SEALED INTERACTIVE BASELINE`

This promotes the combined runtime corrections proven in the 2026-08-30 interactive evidence:

1. Game Pak ROM mirror native-entry dispatch compatibility fix.
2. Native IWRAM `IntrMain` root at `0x03000000` backed by ROM `0x09E864A0`.
3. Native post-handler continuation root `IntrMain_RetAddr` at `0x0300012C` backed by ROM `0x09E865CC`.

Generated dispatch proof includes:

- `{0x03000000u, 0u, 0u, gf_IntrMain}`
- `{0x0300012Cu, 0u, 0u, gf_IntrMain_RetAddr}`

The continuation body also exposes later interior resume aliases from `0x03000130` onward, as generated by the normal recompiler.

## Evidence

Interactive evidence archive:

`SOULGOLD_S0_C3H_EVIDENCE_20260830_025304.zip`

SHA-256:

`ba39d4d122376fe2b7d04510a99edb4a3234d9456b6e433a9db875ed3e5a1338`

Runtime:

- `RUN_EXIT_CODE=0`
- ~201 seconds of captured interactive presentation
- 11,996 presentation samples
- mean frame gap: ~16.759 ms (~59.67 FPS)
- median frame gap: ~16.720 ms (~59.81 FPS)
- >25 ms gaps: 21 / 11,995 (~0.175%)
- >33.3 ms gaps: 6 / 11,995
- median present block: ~1.466 ms

User manual validation:

- BGM normal / clean
- no perceived lag
- indoor traversal works
- outdoor traversal works
- scripted events work
- battle entry works

## Not yet static-complete

Coverage is still:

`NOT_STATIC`

Captured counts:

- distinct misses: 356
- interpreted instructions: 327,789,864
- healed native: 601
- native calls: 539,876
- failed self-heal sites: 82
- jump-table candidate regions: 33

Highest unresolved hot sites include:

- `0x030011E8` THUMB, bridged x12001, not healed
- `0x0300017C` ARM, bridged x231, not healed
- dynamic/jump-table regions around `0x030075xx..0x03007Dxx`

These move to S0-D static-coverage closure and must not invalidate this interactive correctness baseline unless regression evidence proves otherwise.

## Visual fidelity issues opened in parallel

Scheme A = faithful/original presentation.

Open V0 items:

1. New Bark Town lamp glow anchor appears visually offset from the lamp head.
2. Continuous overworld character shadows should be removed in favor of vanilla shadow behavior.

Source audit confirms pinned SoulGold currently has:

`#define OW_OBJECT_VANILLA_SHADOWS FALSE`

The project DNS documentation states `TRUE` restores regular/vanilla shadows (normally no permanent standing shadow; jumping behavior remains), although DNS interaction must be smoke-tested.

New Bark Town contains four `OBJ_EVENT_GFX_LIGHT_SPRITE` object events using `LIGHT_TYPE_BALL` (`trainer_sight_or_berry_tree_id = 0`). Lamp position must be corrected by source/map evidence rather than guessed globally.

Visual polish is a parallel branch and must not block S0-D/T0/A0 progress.

## Next mainline

Parallel lanes after C3H:

- **S0-D0**: static coverage prioritization / hot persistent RAM-code classification.
- **T0**: Traditional Chinese text/font/localization architecture audit.
- **V0-A**: faithful overworld visual audit (vanilla shadows + lamp anchor).
- **A0**: AYN THOR / Android ARM64 host/toolchain preparation after the first D0/T0 evidence pass.

## Permanent requirements

1. Every meaningful checkpoint ships a downloadable handoff.
2. Finished product ships Traditional Chinese `zh-Hant-TW` with English fallback.
3. Primary finished hardware target is AYN THOR / Android ARM64.
4. PC/WSL is a development and diagnostic host, not the final runtime target.
